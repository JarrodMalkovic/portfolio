query HomePageContent {
	projects {
		title
		description
		githubLink
		websiteLink
		thumbnail {
			url
		}

		skills {
			name
		}
	}

	skills {
		name
	}

	works(orderBy: order_ASC) {
		description
		title

		logo {
			url
		}
	}
}
